"""Phase I package-level default constants.

These defaults are intentionally minimal and will be expanded as legacy
configuration is migrated out of ``pipeline_config.py``.
"""

DEFAULT_PACKAGE_NAME = "MitoRiboPy"
DEFAULT_IMPORT_NAME = "mitoribopy"

